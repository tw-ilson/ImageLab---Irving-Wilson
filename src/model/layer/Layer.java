package model.layer;

/**
 * An interface for representing an individual layer, veritably an image to be edited by the
 * program.
 */
public interface Layer {

  /**
   * makes the layer invisible.
   */
  public Layer changeVisibility();


}
